// Minimal root layout required by Next.js — locale layout at [locale]/layout.tsx handles all rendering
export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children
}
