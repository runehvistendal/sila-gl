---
description: Sila — sessionstatus, næste fokus og vigtige beslutninger (hold synk med hukommelse.md)
alwaysApply: true
---

# Sila.gl — agent-hukommelse (spejler hukommelse.md)

Fuld udviklerkontekst: **repo-roden [CLAUDE.md](CLAUDE.md)**. Følgende er kort status til sessioner.

## Status (29.4.2026)

- `/hytter`: facilitetsfiltre i Filtrer-popover (AMENITY_FILTER_KEYS), HytterClient.tsx
- `/hytter/[id]`: to-kolonne layout, sticky booking, CabinDetailLayout.tsx. Rækkefølge: Om hytten → Inkluderet → Din vært → Kom dertil → Anmeldelser
- `/transport`: Filtrer-knap med popover (bådtype, kabine, ledige pladser)
- `/transport/[id]` + `/hytter/[id]`: TransportDrawer.tsx (returture via shadcn Sheet)
- `/dashboard`: BookingRow accordion, Mine ønsker + Gæsteønsker med cabin_requests
- `/anmod`: hytteanmodnings-formular (ny side + server action)
- DB: cabin_requests tabel + RLS oprettet
- Ingen kendte bugs.

## Næste fokus

- **Footer** — se CLAUDE.md, sektionen «Næste i rækkefølge».

## Vigtige beslutninger (gældende)

- **/profil:** lyst design (`bg-gray-50`), ikke mørkt som resten.
- **Roller:** brugeren vælger aktivt; kan skifte når som helst.
- **Telefon:** react-phone-number-input, E.164, `defaultCountry: GL`.
- **Email-skift:** `supabase.auth.updateUser` med bekræftelse til ny adresse.
- **Navbar sprog + avatar:** `NavUser` + `revalidatePath("/")` (ingen særskilt client-fetch til sprog).
- **Cloudinary:** signed uploads, `sila/avatars` og `sila/cabins/[id]`.
- **amenityMeta.ts:** AMENITY_META (display) + AMENITY_FILTER_KEYS (18 DB-nøgler til filterpanel).
- **TransportDrawer props:** `id` (ride_share_id), `seats`, `onClose`.

## Ikke nu (backlog / Fase 3)

- IP-geolocation til sprog/lokation.
- next-intl: ruter `/da/`, `/en/`, `/kl/`.
- Kalaallisut (`kl`): JSON-oversættelser, involvér lokale.

*Opdatér denne regel sammen med [hukommelse.md](hukommelse.md) når status eller beslutninger ændres.*
