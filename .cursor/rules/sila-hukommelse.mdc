---
description: Sila — sessionstatus, næste fokus og vigtige beslutninger (hold synk med hukommelse.md)
alwaysApply: true
---

# Sila.gl — agent-hukommelse (spejler hukommelse.md)

Fuld udviklerkontekst: **repo-roden [CLAUDE.md](CLAUDE.md)**. Følgende er kort status til sessioner.

## Status (6.5.2026)

- **3 % servicegebyr (gæst):** `service_fee_ore` på `cabin_bookings`, `ride_share_bookings`, `transport_offers` · migration **`20260506000000_service_fee.sql`** · `calcServiceFee()` i `money.ts` · checkout: `createCabinBooking`, `/api/transport/checkout`, `acceptTransportOffer`; `application_fee_amount` = kommission **+** servicegebyr · UI: CabinBookingWidget, TransportDetailClient, Drawer; bundlet `cabins.*` / `transport.*` for servicetekst · **immutable** `service_fee_ore` på cabin_bookings via trigger
- **`/udbyderguide`:** lokalt på `src/app/[locale]/udbyderguide/page.tsx` — hero-flow (Megaphone / Users / Banknote), CTA+nederst i økonomi, **mobile-first** hele siden · betaling-afsnit fjernet tidligere
- Øvrigt uændret ift. huske-liste nedenfor (5.5-basis)

## Bygget og komplet

- **PostHog analytics** ✅ — `posthog-js`; `PostHogProvider.tsx` init kun med `NEXT_PUBLIC_POSTHOG_KEY` + `NEXT_PUBLIC_POSTHOG_HOST`; `[locale]/layout.tsx`; events i `posthog-events.ts`; `.env.local` + Vercel før lancering

---

## Status (5.5.2026 — udvidet med 6.5 ovenfor)

- `/admin`: dashboard, brugere, hytter, bookinger, anmodninger — `is_admin` + `proxy.ts` + service_role
- **`/studio`:** Sanity Studio (`is_admin`; håndteres i `proxy.ts` før next-intl — intet `/da`-prefix)
- **Sanity — Visual Editing + Page Builder:** 5 sektionstyper (`heroSection`, `textImageSection`, `faqSection`, `ctaSection`, `imageGallerySection`) · `SectionRenderer.tsx` + blokke under `src/components/sanity/sections/`
- **CMS:** Dynamisk **`[slug]/page.tsx`** · **Stega** + Presentation → click-to-edit blyant · `getSanityPublicClient()` + `getSanityDraftStegaClient()` i `src/lib/sanity.ts`
- **`globalSettings`:** redaktør- og UI-tekster (63 navngivne felter + mange **`stringOverrides`**, fx ~707 efter seed fra bundlet JSON) · `mergeSanityIntoMessages` + **`scripts/seed-sanity-content.ts`** (`npm run seed:sanity`; **`SANITY_API_TOKEN` med Editor**, ikke read-only)
- **`sanityHref.tsx`:** interne links via `@/i18n/navigation`, eksterne via `<a>`
- **Presentation:** `defineDocuments` + `defineLocations` for forside (**`globalSettings` + `homePage`**), destination, page, blog post
- **Forsiden:** **3 nyeste publicerede hytter** fra Supabase (ikke mock)
- SEO grundlag: metadata, sitemap, robots, `/destination/[slug]`, JsonLd
- `/opret/baad`: to knapper — "Gem båd" + "Gem og opret tur →"
- `/opret/samsejlads`: ny side, tur/retur-pris (primær) + enkelttur 60% auto-forslag
- `/opret/hytte`: billeder + alle felter på én side (PendingCabinImageUpload)
- `/opret/hytte/[id]/tilgaengelighed`: kalender (klik-baseret periodevalg, ingen drag), hover-preview, settings-sektion (min_nights + preparation_days), tre gem-knapper
- `cabin_availability`: gemmer BLOKEREDE datoer (is_available=false) — ledige er default
- `saveAvailability` / `saveAll`: returnerer `{ redirectTo }` / `{ error }` — aldrig `redirect()` direkte
- `saveAll`: gemmer settings + tilgængelighed + publicering i ét kald (bag "Gem og publicér →")
- `min_nights` + `preparation_days` på cabins — migration pushet, validering i bookings.ts + CabinBookingWidget
- `preparation_days`: datoer blokeres i /hytter/[id] bookingkalender efter booking check_out
- Knaplogik Mine hytter (dashboard + /opret): Kladde → Publicér (grøn). Aktiv → Afpublicér (amber, AlertDialog) + Dupliker
- "Udlej nu/denne" fjernet — publicering via Publicér-knap
- Billede + navn klikbart på hytte-rækker: aktiv → /hytter/[id], kladde → /opret/hytte/[id]/rediger
- Øje-symbol fjernet fra hytte-rækker
- Rediger-siden: kun HytteForm + "Administrer tilgængelighed →" link (kalender fjernet)
- AlertDialog (shadcn/ui) installeret
- Mappenavne: æ/ø/å forbudt i routes — brug ae/oe/aa (fx tilgaengelighed)

## i18n + tekst

- **Bundlet fallback:** `src/i18n/bundled/da.json` · `src/i18n/bundled/en.json` · `mergeSanityIntoMessages` henter overlays fra Sanity `globalSettings`

## Næste fokus

- **Stripe live-test end-to-end** (**inkl. 3 %-linje**) · derefter lancering — se `hukommelse.md`

## Vigtige beslutninger (gældende)

- **/profil:** lyst design (`bg-gray-50`), ikke mørkt som resten.
- **Roller:** brugeren vælger aktivt; kan skifte når som helst.
- **Telefon:** react-phone-number-input, E.164, `defaultCountry: GL`.
- **Email-skift:** `supabase.auth.updateUser` med bekræftelse til ny adresse.
- **Navbar sprog + avatar:** `NavUser` + `revalidatePath("/")` (ingen særskilt client-fetch til sprog).
- **Cloudinary:** signed uploads, `sila/avatars` og `sila/cabins/[id]`. Pending upload: `sila/cabins/pending`.
- **amenityMeta.ts:** AMENITY_META (display) + AMENITY_FILTER_KEYS (18 DB-nøgler til filterpanel).
- **TransportDrawer props:** `id` (ride_share_id), `seats`, `onClose`.
- **cabin_availability:** gemmer BLOKEREDE datoer (is_available=false) — ledige er default.
- **Admin / proxy:** service_role key til is_admin-tjek i `proxy.ts` (anon-key blokeres af RLS).
- **Samsejlads pris:** tur/retur primær → enkelttur = 60% auto-forslag (kan overskrives).
- **AvailabilityCalendar settings:** min_nights (1-30) + preparation_days (0/1/2/3) gemmes via saveCabinSettings (hurtig-gem) eller saveAll (kombineret med tilgængelighed).
- **Publish/unpublish:** `publishCabin` + `unpublishCabin` i `src/app/dashboard/actions.ts`.
- **`SANITY_API_TOKEN`:** skal have skrive-/Editor‑ret til seed og mutationer (ikke read-only Viewer alene til seed).

## Ikke nu (backlog / Fase 3)

- IP-geolocation til sprog/lokation.
- next-intl: ruter `/da/`, `/en/`, `/kl/`.
- Kalaallisut (`kl`): JSON-oversættelser, involvér lokale.

*Opdatér denne regel sammen med [hukommelse.md](hukommelse.md) når status eller beslutninger ændres.*
